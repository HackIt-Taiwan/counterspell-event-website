// src/utils/sizeConstants.ts

/**
 * Constants for controlling the size of visual elements such as Planet and UFO.
 * These constants can be reused across multiple components to maintain consistency.
 */

export const SIZE_CONSTANTS = {
    size: 'clamp(300px, 90vw, 2000px)',  // 統一的大小設定
};
